# Documentation

This directory contains analysis provenance, rerun instructions, evidence boundaries, the frozen analysis plan, and documented implementation deviations.

The frozen plan retains historical working-directory names to preserve its original record. Current public-repository path mappings are documented in `nbvk/README.md`.
